name: deploy
on:
  workflow_run:
    workflows: [ build ]
    types: [ completed ]
jobs:
  deploy:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: build-artifacts
          path: dist
      - name: Deploy (placeholder)
        run: |
          echo "Esegui deploy su Cloud Run/Render/Vercel qui"
